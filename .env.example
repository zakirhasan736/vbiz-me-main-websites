# ── VPS / production ─────────────────────────────────────────────────────────
# Put this file on the SERVER as `.env` (project root, next to package.json).
# After editing, RESTART the Node process (pm2 restart, docker restart, etc.).

GEMINI_API_KEY=""

# Optional: client-side key for localhost Live Agent (production uses /api/live-agent/config)
NEXT_PUBLIC_GEMINI_API_KEY=""

# Optional: Gemini Live voice (men's prebuilt voices — see /ai-voices on site)
LIVE_AGENT_VOICE="Puck"
NEXT_PUBLIC_LIVE_AGENT_VOICE="Puck"

# vBiz Me public cards API (community directory)
NEXT_PUBLIC_API_URL="https://app.vbizme.com/api"

# Google AI Studio → API key → HTTP referrers (for Live Agent on your domain):
#   https://yourdomain.com/*
#   https://www.yourdomain.com/*
